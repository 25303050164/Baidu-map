# 2026-09-14 本地保留与远端同步记录

- 当前开发分支：`codex/guodingyi-boundary-comparison-v2`。
- 同步前提交：`99bd882`；同步的远端 main 提交：`56c7838`。
- 远端增量：14 个提交，76 个文件；包括 POI 数据层、分页重叠修复、前端交互和 OSM 离线路由。
- 本地原有 7 个新增文件原样保留：5 份国定一社区报告/计划，以及 `backend/tests/test_live_adaptive_800.py`、`backend/tools/live_adaptive_800.py`。
- 备份位置：`D:/CodexBackups/baidu-sync-20260914-110803`；同步前后 7 个文件 SHA-256 一致。
- 版本标记：`snapshot-2026-09-14-sync`，指向包含本记录及本地新增文件的提交；以 Git 标签解析出的完整提交 SHA 为准。

## 验证与限制

- `tests/test_live_adaptive_800.py`：6 passed、1 failed。失败用例为 `test_mock_run_protects_requests_and_secrets[success]`，期望 completed，实际 failed；尚未定位根因。
- 本次只运行离线 Mock 测试，未发出真实百度请求，未执行 OSM 数据下载。
- 为保留本地内容，本次不修复或重写原实验脚本及报告；这是一份同步快照，不是测试全部通过的发布版本。
- 原 `live_adaptive_800.py` 真实执行入口检查算法/业务源码必须与 `a147c81` 一致；当前同步版本不满足该旧基线条件，不应直接用于重跑历史实验。
- 其他本地分支和独立 worktree 不在本次推送范围内。

## 下一步

先在离线环境定位上述失败并明确新实验基线，再推进算法测试。历史实验产物和账本保持不变；新实验使用独立配置与产物目录。
