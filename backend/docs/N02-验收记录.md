# N02 验收记录

验证日期：2026-09-12（Asia/Shanghai）。

**状态：N02 技术验收项已通过，包括一次百度真实请求成功；另一位团队成员的独立复核签字待补充。**

## 仓库和运行环境

- 已执行 `git fetch origin` 和 `git merge --ff-only origin/main`，结果为 Already up to date。
- 基准提交：`d0ca7d2737229295127cfd04170ae0fa714ca3f4`。
- 开发分支：`feat/n02-fastapi-baidu`；原 `.gitignore` 的 `.npm-cache/` 修改已保留。
- Windows、Python 3.12.14；依赖版本见 `backend/requirements.lock.txt`。
- FastAPI 本机监听 `127.0.0.1:8000`；Vite 本机监听 `127.0.0.1:5173`。

## 已验证结果

| 检查 | 实际结果 |
| --- | --- |
| 依赖一致性 | `pip check` 通过，无依赖冲突 |
| 后端自动测试 | 30 项通过；2 条第三方测试客户端弃用提示，无失败 |
| 健康检查 | 配置 AK 后已重启后端并复验：HTTP 200，`status=ok`，`baidu_ak_configured=true`；配置前也已验证缺少 AK 时接口仍正常 |
| 浏览器普通跨域：127.0.0.1:5173 | PASS，浏览器成功读取 HTTP 200 JSON |
| 浏览器预检跨域：127.0.0.1:5173 | PASS，带 Content-Type 的跨域 GET 成功 |
| 浏览器普通跨域：localhost:5173 | PASS，浏览器成功读取 HTTP 200 JSON |
| 浏览器预检跨域：localhost:5173 | PASS，带 Content-Type 的跨域 GET 成功 |
| 非白名单来源 | 自动测试确认无允许来源响应头，预检返回 400 |
| 密钥保护 | `.env` 与 `.venv` 被 Git 忽略；虚拟 AK 的成功、业务失败、HTTP 失败、超时、异常响应和异常文本均未进入输出或捕获日志 |
| 实际 AK 脱敏检查 | 使用真实 AK 在内存中比对本地调用记录、后端源码、配置示例与后端文档，共检查 9 个文件，未发现密钥；未输出 AK 本身 |
| 百度真实请求 | HTTP 200，百度 `status=0`，响应结构有效，`outcome=success`，命令退出码 0 |

浏览器验收使用前端 Vite 服务下的 `/output/n02-health.html`，由 `backend/tools/browser-health.html` 复制生成。没有向前端页面输入 AK。健康接口不调用百度。

## 真实请求证据

团队已确认填写 AK 并配置 IP 白名单。2026-09-12 11:02:39（Asia/Shanghai），通过后端手动命令发出一次百度地点检索 3.0 真实请求，成功返回。此前的 `missing_ak` 记录仅为缺少配置时的失败检查。

实际执行命令（工作目录为 `backend`）：

```powershell
.\.venv\Scripts\python.exe -m app.smoke
```

本地证据已自动追加在 `backend/logs/baidu-smoke.jsonl`（Git 忽略）。以下为实际输出的脱敏记录，UTC 时间与上述北京时间对应：

```json
{
  "timestamp": "2026-09-12T03:02:39.001950+00:00",
  "request_id": "d39cf790-774a-423a-b1ec-df87f6010981",
  "api_path": "/place/v3/around",
  "elapsed_ms": 143,
  "http_status": 200,
  "baidu_status": 0,
  "result_count": 0,
  "outcome": "success"
}
```

本次查询是固定 BD-09 坐标 `39.915,116.404` 周边 1000 米内的药店。百度返回有效空设施数组，满足 N02 的真实调用成功条件；不能据此认定附近没有药店、存在服务盲区或正式设施业务正确。

AK 配置后已重启服务，健康检查读取到 `baidu_ak_configured=true`，并确认 `http://127.0.0.1:5173` 的允许来源响应头正确。该复验不调用百度，未额外消耗百度调用配额。

## 复核清单

- [x] 本地健康检查可访问。
- [x] 服务端 AK 配置入口和 Git 忽略规则已提供。
- [x] 浏览器跨域访问策略已记录并验证。
- [x] 模拟成功和失败路径的脱敏测试通过。
- [x] 团队确认 AK 和 IP 白名单已配置，地点检索权限由实际成功请求验证。
- [x] 至少一次百度真实请求成功：HTTP 200、百度 `status=0`、响应结构有效。
- [x] 补充实际成功的脱敏调用记录。
- [ ] 另一位团队成员复核配置与实际调用记录（姓名、日期待填写）。

配置步骤、失败结果解释和复现命令见 [后端说明](../README.md)。FR-02、FR-03 在 N02 中仅验证接入基础，不代表等时圈、完整设施检索或分析业务已经交付。
